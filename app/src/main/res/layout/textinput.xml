<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="match_parent" android:layout_width="match_parent"
    android:orientation="vertical"
    android:id="@+id/back">

    <Button android:text="@android:string/ok" android:layout_width="100dp" android:id="@+id/send" android:layout_height="wrap_content"
        android:layout_alignTop="@+id/cancel"
        android:layout_toLeftOf="@+id/cancel"
        android:layout_toStartOf="@+id/cancel" />

    <Button
        android:text="@android:string/cancel"
        android:layout_width="70dp"
        android:id="@+id/cancel"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

    <com.maxonsoft.plugin.keyboardmanager.backable
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/textbox"
        android:layout_alignParentBottom="true"
        android:layout_toLeftOf="@+id/send"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:imeOptions="flagNoExtractUi|flagNoEnterAction"
        android:singleLine="true"
        android:textColor="#000000"
        android:layout_alignTop="@+id/send" />

</RelativeLayout>